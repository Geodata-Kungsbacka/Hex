# GeoServer Schema Listener - Python dependencies
# Install with: py -m pip install -r requirements.txt
#
# Note: psycopg2 requires a C compiler on some systems.
# If installation fails, use psycopg2-binary instead:
#   py -m pip install psycopg2-binary
psycopg2>=2.9
requests>=2.28
python-dotenv>=1.0
pywin32>=306
